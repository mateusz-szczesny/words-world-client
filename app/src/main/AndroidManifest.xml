<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.politechnika.szczesny.words_world_client">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" android:theme="@style/AppTheme" />
        <activity android:name=".AppBaseActivity" android:theme="@style/AppTheme" />
        <activity android:name=".LoginActivity" android:theme="@style/AppTheme" />
        <activity android:name=".AboutActivity" android:theme="@style/AppTheme" />
        <activity android:name=".SettingsActivity" android:theme="@style/AppTheme" />
        <activity android:name=".FriendsSearchActivity" android:theme="@style/AppTheme" />
        <activity android:name=".LanguagesActivity" android:theme="@style/AppTheme" />
        <activity android:name=".FriendsActivity" android:theme="@style/AppTheme" />
        <activity android:name=".TabooActivity" android:theme="@style/AppTheme" />
        <activity android:name=".TranslateActivity" android:theme="@style/AppTheme" />
        <activity android:name=".OtherUserActivity" android:theme="@style/AppTheme" />
        <activity android:name=".PreTabooSettingsActivity" android:theme="@style/AppTheme" />
        <activity android:name=".AddTabooCardActivity" android:theme="@style/AppTheme" />
    </application>

</manifest>